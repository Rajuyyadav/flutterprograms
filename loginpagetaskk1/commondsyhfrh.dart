import 'package:flutter/material.dart';

class Commonfirllllllld extends StatelessWidget {
  const Commonfirllllllld({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.only(top: 5),
      child: TextField(
decoration: InputDecoration(
  // hintText: "efjhd",
  // labelText: "nwejgtrdu",
  border: OutlineInputBorder(),
),
      ),
    );
  }
}
