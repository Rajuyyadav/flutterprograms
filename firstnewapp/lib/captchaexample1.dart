import 'package:flutter/material.dart';

class CaptchaExOne extends StatelessWidget {
  const CaptchaExOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Center(),
    );
  }
}
